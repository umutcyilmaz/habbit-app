import { ProtectionInterceptScreen } from "../../src/features/protect/screens/ProtectionInterceptScreen";

export default function ProtectionInterceptRoute() {
  return <ProtectionInterceptScreen />;
}
